sed -i 's/val launchIntent = packageManager.getLaunchIntentForPackage(appInfo.packageName)/if (isFloating) {\n                                AppState.windows.add(\n                                    FloatingWindowData(\n                                        id = AppState.windowIdCounter++,\n                                        title = appInfo.name,\n                                        icon = null,\n                                        appIcon = appInfo.icon,\n                                        offset = androidx.compose.ui.unit.IntOffset(100, 200 + (AppState.windowIdCounter * 50)),\n                                        size = androidx.compose.ui.unit.IntSize(800, 1000),\n                                        color = androidx.compose.ui.graphics.Color(0xFF00E5FF),\n                                        packageName = appInfo.packageName\n                                    )\n                                )\n                            } else {\n                                val launchIntent = packageManager.getLaunchIntentForPackage(appInfo.packageName)/g' /app/applet/app/src/main/java/com/yash/shortw/OverlayService.kt
sed -i 's/startActivity(launchIntent)/startActivity(launchIntent)\n                                }/g' /app/applet/app/src/main/java/com/yash/shortw/OverlayService.kt
